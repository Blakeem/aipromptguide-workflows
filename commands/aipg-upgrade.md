---
description: AIPG · run the upgrade-cycle dynamic workflow — breadth-spanning migration across many call sites.
---
The user wants to run the **upgrade-cycle** dynamic workflow on the task below. Its instructions live in
`aipg/workflows/upgrade/` — or, if that folder is absent, in the AI Prompt Guide workflows checkout
wherever it lives (default clone folder `aipromptguide-workflows/`).

$ARGUMENTS
