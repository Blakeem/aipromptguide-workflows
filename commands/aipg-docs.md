---
description: AIPG · run the docs-cycle dynamic workflow — copy the docs a project needs verbatim (web/repo/files), curate + index them into a working folder.
---
The user wants to run the **docs-cycle** dynamic workflow for the need below. Its instructions live
in `aipg/workflows/docs/` — or, if that folder is absent, in the AI Prompt Guide workflows checkout
wherever it lives (default clone folder `aipromptguide-workflows/`).

$ARGUMENTS
