---
description: AIPG · run the investigate-cycle dynamic workflow — search for an answer that already exists and qualify it against fixed pass/fail criteria.
---
The user wants to run the **investigate-cycle** dynamic workflow on the question below. Its instructions
live in `aipg/workflows/investigate/` — or, if that folder is absent, in the AI Prompt Guide workflows
checkout wherever it lives (default clone folder `aipromptguide-workflows/`).

$ARGUMENTS
