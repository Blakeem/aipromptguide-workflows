---
description: AIPG · run the debug workflow — find production defects and/or fix a verified issue inventory (triage → batched fixes).
---
The user wants to run the **debug** workflow on the task below. Its instructions live in
`aipg/workflows/debug/` — or, if that folder is absent, in the AI Prompt Guide workflows checkout
wherever it lives (default clone folder `aipromptguide-workflows/`).

$ARGUMENTS
