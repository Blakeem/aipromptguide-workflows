---
description: AIPG · run the review-cycle dynamic workflow — production-readiness review, triage then batched fixes.
---
The user wants to run the **review-cycle** dynamic workflow on the task below. Its instructions live in
`aipg/workflows/review/` — or, if that folder is absent, in the AI Prompt Guide workflows checkout
wherever it lives (default clone folder `aipromptguide-workflows/`).

$ARGUMENTS
