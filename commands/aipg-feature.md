---
description: AIPG · run the feature-cycle dynamic workflow — build one bounded feature, plan-first and staged.
---
The user wants to run the **feature-cycle** dynamic workflow on the task below. Its instructions live in
`aipg/workflows/feature/` — or, if that folder is absent, in the AI Prompt Guide workflows checkout
wherever it lives (default clone folder `aipromptguide-workflows/`).

$ARGUMENTS
