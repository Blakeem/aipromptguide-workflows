---
description: AIPG · run the brainstorm-cycle dynamic workflow — fan out one fully-committed variation per lens for you to compare.
---
The user wants to run the **brainstorm-cycle** dynamic workflow on the task below. Its instructions live
in `aipg/workflows/brainstorm/` — or, if that folder is absent, in the AI Prompt Guide workflows checkout
wherever it lives (default clone folder `aipromptguide-workflows/`).

$ARGUMENTS
